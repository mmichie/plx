[package]
name = "plx"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
git2 = "0.20"

[dev-dependencies]
tempfile = "3"

[lints.clippy]
pedantic = { level = "warn", priority = -1 }

[profile.release]
opt-level = 3
lto = true
strip = true
